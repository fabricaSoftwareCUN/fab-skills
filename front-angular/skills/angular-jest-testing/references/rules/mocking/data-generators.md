# Mock Data Generator

Use **faker** to generate mock data.

---

## Rule

If mocking component data:

1. Check `shared/mocks`
2. If not exists → create:

```
<component-name|service-name|other-name>.mock.ts
```
